/**
 * Shared header gradient. The two stops sit closer together than the brand pair
 * used on buttons, so a full-width header reads as a backdrop rather than a
 * saturated band competing with page content.
 */
export const HEADER_GRADIENT = ["#4A62E4", "#6F51D8"] as const;
